import Home from "../components/Home"
import Login from "../components/Login"
const routes = [
    {
        path: '/',
        element: Home
    },
    {
        path: '/login',
        element: Login
    }
]

export default {
    routes
}